import { default as Container } from './grid-container';
import { default as Row } from './grid-row';
import { default as Col } from './grid-col';

export const Grid = {
  Container,
  Row,
  Col,
};
